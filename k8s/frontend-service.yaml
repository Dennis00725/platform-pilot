apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: platform-pilot-frontend
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000
    protocol: TCP
  selector:
    app: platform-pilot-frontend
