services:
  backend:
    build: ./platform-pilot-backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production

  frontend:
    build: ./platform-pilot-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
